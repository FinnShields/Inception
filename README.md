# Inception
A project introducing Docker, setting up a small environment with Docker Compose.
The project should be run inside a virtual machine, and involves the following tools and technologies:
Docker, MariaDB, Nginx, and Wordpress. The goal is to create and manage a small wordpress site, storing the
site data in MariaDB.
